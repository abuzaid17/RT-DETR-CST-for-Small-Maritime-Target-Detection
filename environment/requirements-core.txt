# ============================================================
# RT-DETR-CST — Core Dependencies
# Full environment snapshot: environment.lock
# ============================================================

# Deep learning framework
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# Detection framework (Ultralytics RT-DETR)
# NOTE: originally installed via `pip install git+https://github.com/ultralytics/ultralytics.git`
# confirmed version via `ultralytics.__version__` = 8.4.146
ultralytics==8.4.146

# Custom-module dependency (DropPath used in SWNBlock)
timm==1.0.29

# Numerical / array operations
numpy==2.1.3

# Image I/O and processing
opencv-python==5.0.0.93
pillow==11.3.0

# Augmentation pipeline
albumentations==2.0.8

# Dataset manifest / manipulation
pandas==2.2.3

# VOC XML annotation parsing
lxml==6.1.2

# Config files
PyYAML==6.0.3

# Progress bars during training
tqdm==4.67.3